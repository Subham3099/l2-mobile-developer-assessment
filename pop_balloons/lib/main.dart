import 'package:flutter/material.dart';
import 'start_game_page.dart'; // Import the start game page

void main() {
  runApp(const MaterialApp(
    home: StartGamePage(), // Display the start game page when the app launches
  ));
}
